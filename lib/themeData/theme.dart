import 'dart:ui';
import 'package:flutter/material.dart';

const Color colorPrimary = Color(0xFFFFA300);
const Color colorPrimVar = Color(0xFFffe768);
const Color colorSecondary = Color(0xFF54bfb5);
Theme(
  data: ThemeData( 
  primaryColor: colorPrimary,)
);